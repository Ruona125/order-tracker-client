import ModifyMilestoneComponent from "../../components/modify-milestone/ModifyMilestoneComponent";

const ModifyMilestone = () => {
  return (
    <div>
      <ModifyMilestoneComponent />
    </div>
  );
};

export default ModifyMilestone;
